package com.example.elrondweather.Helper;

public class WeatherToJSON
{
   //public WeatherToJSON(Context context) { this.context = context;}
   //Context context;
   //String locationName = CurrentLocation.getLocationName();
   //String result;
   //static String lat = String.valueOf(CurrentLocation.getLocation().getLatitude());
   //static String lon = String.valueOf(CurrentLocation.getLocation().getLongitude());

   //@Override
   //protected void finalize() throws Throwable {
   //    super.finalize();
   //    WeatherToJSON weatherToJSON = new WeatherToJSON(context);
   //}




        //ReadFromFile readFromFile = new ReadFromFile(context);
        //WeatherRoot weatherRoot = new WeatherRoot();
        //Weather weather = new Weather();
        //Current current = new Current();
        //String json = String.valueOf(WeatherAPICall.result);
        //Moshi moshi = new Moshi.Builder().build();
        //JsonAdapter<WeatherRoot> jsonAdapter = moshi.adapter(WeatherRoot.class);
        //try {
        //    result = String.valueOf(jsonAdapter.fromJson(json));
        //} catch (IOException e) {
        //    e.printStackTrace();
        //}
        //weather.setMain(result);
        //weatherRoot.setCurrent(current);
        //List<Daily> daily = null;
        //List<Hourly> hourly = null;
        ////daily.add(weather.);
        ////weatherRoot.setDaily();
        //weatherRoot.setHourly(hourly);
        ////Log.e("asd", String.valueOf(weatherRoot.getDaily().size()));


   // }


}
